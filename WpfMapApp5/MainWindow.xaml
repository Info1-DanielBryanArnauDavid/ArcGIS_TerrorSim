<Window x:Class="ArcGIS_App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
        Title="ArcGIS TerroSim" WindowState="Maximized" WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Style for Menu -->
        <Style TargetType="Menu">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="#B0F4F4F4"/>
            <Setter Property="Height" Value="23"/>
        </Style>

        <!-- Style for Menu Items -->
        <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,5,0"/>
        </Style>
    </Window.Resources>

    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top" Name="MainMenu">
            <MenuItem Header="File" Padding="11,5">
                <MenuItem Header="New" Click="FileNew_Click" />
                <MenuItem Header="Open" Click="FileOpen_Click" />
                <MenuItem Header="Exit" Click="FileExit_Click" />
            </MenuItem>
            <MenuItem Header="Add" Padding="11,5">
                <MenuItem Header="Add Terrain" Click="AddTerrain_Click" />
                <MenuItem Header="Add Polyline" Click="AddPolyline_Click" />
            </MenuItem>
            <MenuItem Header="View" Padding="11,5">
                <MenuItem Header="Reset View" Click="ResetView_Click" />
                <MenuItem Header="Zoom In" Click="ZoomIn_Click" />
                <MenuItem Header="Zoom Out" Click="ZoomOut_Click" />
            </MenuItem>
        </Menu>

        <!-- Main Content (SceneView) -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="223*"/>
                <ColumnDefinition Width="98*"/>
                <ColumnDefinition Width="320*"/>
            </Grid.ColumnDefinitions>
            <esri:SceneView x:Name="MySceneView" Grid.ColumnSpan="3" />
        </Grid>
    </DockPanel>
</Window>
